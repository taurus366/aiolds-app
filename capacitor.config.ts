import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'aiolds.app',
  appName: 'aiolds-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
